# Database Type Selection (Factory Pattern)
# Options: postgres, inmemory
# Change this to switch database implementations without code changes.
DB_TYPE=postgres

# PostgreSQL Configuration (only used when DB_TYPE=postgres)
DB_HOST=db
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=admin
DB_NAME=library_db

DB_SSLMODE=disable

# Application Configuration
APP_PORT=8080
GIN_MODE=debug